# 会里｜脱敏演示包

这是给发布视频和现场演示使用的独立示例。页面延续会里原有的纸感、宋体标题、朱红强调和导航；示例中的项目、会议、行动和 Agent 对话均为全新虚构内容，不连接作者账号或任何真实飞书资料。项目页的“社区图书角”展示一件事如何跨会议和 Agent 对话归拢，以及一个未被回复的决策如何单独回到“待我处理”。这个示例是静态模拟，不能证明自动语义归类已在接收者环境运行。

“此刻”现展示「甜有三种走法」，抽屉里有「句点暂留」「一滴比一场雨贵」「镜子各忙各的」等四件作品。「甜有三种走法」和「镜子各忙各的」保留了作者个人版的原图，已移除图片元数据；其他画面为演示重绘。所有正文、出现原因、依据和关联会议均改写为虚构示例。反馈、保存、丢弃和“再来一件”只在当前浏览器页面内操作预置作品，刷新会重置，不调用 Agent 生成。

## 直接看示例

需要 Node.js 22.12+：

```bash
npm ci
npm run demo
```

打开 `http://127.0.0.1:8136/` 查看“此刻”，点右上角 `···` 打开留白抽屉；切到“项目”后，点“社区图书角”查看项目详情。页面顶部始终标记“虚构示例”。随手记和留白操作只在当前页面保留，刷新后重置；连接 Agent、实际写入与后台汇总都需要使用者的 Agent 另行配置。

## 现场讲解顺序

1. 此刻：看「甜有三种走法」原图，打开抽屉浏览四件留白，其中「镜子各忙各的」也保留了个人版原图；周围的解释和依据均为虚构演示内容。
2. 日历：从一场会进入。
3. 项目：同一件事也可能散在另一场会和 Agent 对话里，按目标与交付物归拢，不按标题硬合并。
4. 待我处理：虚构对话里提出的“20 本还是 40 本”仍未获答复，项目页单独提醒，保留来源与时间。
5. 接手：已有工作台时，使用者授权 Agent 读取并在原风格里融入必要界面；没有时从会里前端参考起步。两种路径都需 Agent 检查资料权限、接入并用一场真实会议验收。

请勿把此演示说成已经连接真实飞书、已支持任意工作台自动融合，或已验证其他用户的 Agent。视频使用的个人“今日留白”截图仅放在私有剪辑工程中，不包含在这个演示包。
