# v0.1.0 验证记录

验证日期：2026-09-15。

## 已完成

- 前端 TypeScript 检查、真实接入模式构建和虚构示例模式构建通过。
- 4 项前端行为测试通过：责任失效降级、缺来源降级、等待对象区分、日历溢出与排序。
- 17 项连接器测试通过：STDIO 握手、工具发现、参数约束、账号漂移、重定向、配对防重、凭据不回显等。测试使用合成输入与隔离目录。
- 4 项隐私扫描器测试通过；公开源码、配置、文档与私有术语补充扫描未发现命中。
- 依赖审计：锁定当前依赖后，官方 npm registry 报告 0 项已知漏洞；这是此日期的结果，不代表后续不会出现新通告。
- 本地 Tabbit 实测七个主导航页面、示例随手记保存/完成/撤销；390px 项目页无横向溢出。
- 项目页加载、空、403、503 与重试恢复已用本地隔离接口响应验证；读取失败时不呈现“没有资料”的空结果。
- 项目预览只用虚构的项目与会议，未使用真实画像和业务记录；PNG 没有文本/EXIF 元数据。原生生成海报已目视检查主要文字与公开定位。
- 文档相对链接检查通过，未包含内部域名、账户标识、私有路径、原模型固定选择或平台私有 SDK。

## 已知提示与限制

构建存在第三方模块 `use client` 被忽略及产物体积提示；构建成功。浏览器偶有通用 `Object` 脚本告警，功能验证结果单列，不声称零告警。

没有验证另一位用户或其他 Agent 的真实登录与全链路运行；没有交付生产数据库、认证、常驻后台或跨用户服务。示例操作不证明真实保存、权限隔离或业务执行完成。

接入时请按 [验收表](../references/acceptance.md) 使用本人资料验证。公开仓库的 CI 还会重新执行构建、测试与扫描。
